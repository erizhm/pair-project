<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Profil - Pop</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  
  <style>
    :root { --pop-color: #f44336; --pop-hover: #d32f2f; }
    body { background-color: #f8f9fa; }
    .btn-pop { background-color: var(--pop-color); color: white; border-radius: 50px; font-weight: 600; border: none; }
    .btn-pop:hover { background-color: var(--pop-hover); color: white; }
    .card-edit { border: none; border-radius: 1rem; }
  </style>
</head>
<body>

  <div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-md-6">
            
            <a href="/profile" class="text-decoration-none text-muted mb-3 d-block">&larr; Kembali ke Profil</a>

            <div class="card card-edit bg-white shadow-sm">
                <div class="card-body p-5">
                    <h3 class="fw-bold mb-4">Edit Profil</h3>

                    <% if (typeof errors != 'undefined' && errors.length > 0) { %>
                        <div class="alert alert-danger" role="alert">
                            <strong>Ups, ada masalah!</strong>
                            <ul class="mb-0 ps-3">
                                <% errors.forEach(err => { %>
                                    <li><%= err %></li>
                                <% }) %>
                            </ul>
                        </div>
                    <% } %>
                    
                    <form action="/profile/edit" method="post">
    
                        <div class="mb-4">
                            <label class="form-label text-muted fw-bold">Username</label>
                            <input type="text" name="username" class="form-control form-control-lg bg-light border-0" 
                                value="<%= profile ? profile.username : '' %>" required>
                        </div>

                        <div class="mb-4">
                            <label class="form-label text-muted fw-bold">Bio</label>
                            <textarea name="bio" class="form-control bg-light border-0" rows="3" placeholder="Ceritakan sedikit tentang dirimu..."><%= profile ? profile.bio : '' %></textarea>
                            <div class="form-text">Maksimal 200 karakter.</div>
                        </div>

                        <div class="d-grid gap-2 mt-5">
                            <button type="submit" class="btn btn-pop btn-lg">Simpan Perubahan</button>
                            <a href="/profile" class="btn btn-light btn-lg text-muted">Batal</a>
                        </div>

                    </form>
                </div>
            </div>

        </div>
    </div>
  </div>

</body>
</html>
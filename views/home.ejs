<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Timeline Dashboard - Pop</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
  
  <style>
    :root {
      --pop-color: #f44336;
      --pop-hover: #d32f2f;
    }
    body { background-color: #f8f9fa; }
    .text-pop { color: var(--pop-color) !important; }
    
    .btn-pop {
      background-color: var(--pop-color);
      border-color: var(--pop-color);
      color: white;
      font-weight: 600;
      border-radius: 50px;
    }
    .btn-pop:hover {
      background-color: var(--pop-hover);
      border-color: var(--pop-hover);
      color: white;
    }

    .btn-fab {
        position: fixed;
        z-index: 1000;
        bottom: 60px; 
        right: 80px;  

        padding: 10px 25px; 
        border-radius: 50px; 
        display: flex;
        align-items: center;
        gap: 5px; 

        background-color: var(--pop-color);
        color: white;
        border: none;
        box-shadow: 0 4px 15px rgba(244, 67, 54, 0.4);
  
        transition: all 0.3s ease;
    }

    .btn-fab:hover {
        transform: translateY(-5px);
        background-color: var(--pop-hover);
        color: white;
    }

    @media (max-width: 1200px) {
        .btn-fab {
            right: auto; 
            left: 50%;
            transform: translateX(-50%);
            bottom: 40px; 
        }

        .btn-fab:hover {
            transform: translateX(-50%) translateY(-5px); 
        }
    }

    .nav-link-custom {
        color: #555;
        text-decoration: none;
        font-weight: 600;
        transition: color 0.2s;
        display: flex;
        align-items: center;
        gap: 5px;
    }
    .nav-link-custom:hover {
        color: var(--pop-color);
    }

    .profile-link {
        color: #555;
        transition: color 0.2s;
    }
    .profile-link:hover {
        color: var(--pop-color) !important;
    }

    .card-post {
      border: none;
      border-radius: 1rem;
      transition: transform 0.2s;
    }
    
    .hashtag-container a {
      color: var(--pop-color);
      text-decoration: none;
      font-weight: 600;
    }
    .hashtag-container a:hover { text-decoration: underline; color: var(--pop-hover); }
  </style>
</head>
<body>

  <nav class="navbar navbar-expand-lg navbar-light bg-white sticky-top shadow-sm">
    <div class="container">
      
      <a class="navbar-brand fw-bold fs-3 text-pop" href="/">Pop.</a>

      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
        <div class="d-flex align-items-center gap-3 mt-3 mt-lg-0">
            
            <a href="/" class="nav-link-custom">
                <i class="bi bi-house-door-fill fs-5"></i> 
                <span>Beranda</span>
            </a>

            <div class="vr d-none d-lg-block mx-2"></div>

            <span class="text-muted small d-flex align-items-center gap-1">
                Hai, 
                <a href="/profile" class="text-decoration-none fw-bold profile-link d-flex align-items-center gap-1">
                    <%= currentUser.Profile ? currentUser.Profile.username : currentUser.email.split('@')[0] %>
                    <i class="bi bi-person-circle"></i> 
                </a>
            </span>
            
            <a href="/logout" class="btn btn-outline-secondary border-0 btn-sm ms-2" title="Logout">
                <i class="bi bi-box-arrow-right fs-5"></i>
            </a>

        </div>
      </div>

    </div>
  </nav>

  <a href="/pop/add" class="btn btn-pop btn-fab" title="Buat Postingan Baru">
    <i class="bi bi-plus-lg display-6" style="font-size: 1.1rem;"></i>
    <span>Pop.!</span>
  </a>

  <section class="py-5">
    <div class="container">
      <div class="row justify-content-center">
        <div class="col-lg-8">

            <form action="/" method="get" class="mb-4">
                <div class="input-group shadow-sm rounded-pill overflow-hidden bg-white border">
                    
                    <input type="text" name="search" class="form-control border-0 py-2 ps-4" 
                           placeholder="Cari postingan seru..." 
                           value="<%= search || '' %>"> <button class="btn btn-white border-start text-secondary px-3" type="submit">
                        <i class="bi bi-search"></i>
                    </button>

                    <% if (search) { %>
                        <a href="/" class="btn btn-danger text-white px-3 d-flex align-items-center" title="Hapus Pencarian">
                            <i class="bi bi-x-lg"></i>
                        </a>
                    <% } %>

                </div>
            </form>
            
            <h4 class="fw-bold mb-4 text-secondary"><i class="bi bi-card-heading"></i> Timeline</h4>

            <% if (posts.length === 0) { %>
            <div class="text-center py-5 bg-white rounded-4 shadow-sm card-post">
                    
                <% if (search) { %>
                    <div class="display-1 mb-3">🔍</div>
                    <h3 class="fw-bold text-muted">Tidak ditemukan</h3>
                    <p class="text-muted">Tidak ada postingan yang cocok dengan "<strong><%= search %></strong>"</p>
                    <a href="/" class="btn btn-outline-pop mt-2 rounded-pill px-4">Kembali ke Timeline</a>
                    
                <% } else { %>
                    <div class="display-1 mb-3">📭</div>
                    <h3 class="fw-bold text-muted">Sepi banget...</h3>
                    <p class="text-muted">Jadilah yang pertama memposting!</p>
                    <a href="/pop/add" class="btn btn-outline-pop mt-2">Mulai Posting</a>
                <% } %>

            </div>
            <% } else { %>
                
                <% posts.forEach(post => { %>
                    <div class="card card-post bg-white shadow-sm mb-4">
                        <div class="card-body p-4">
                            
                            <div class="d-flex justify-content-between align-items-center mb-3">
                                <div class="d-flex align-items-center gap-2">
                                    <div class="rounded-circle bg-light d-flex align-items-center justify-content-center text-pop fw-bold border" style="width: 45px; height: 45px; font-size: 1.2rem;">
                                        <%= post.User.Profile ? post.User.Profile.username[0].toUpperCase() : post.User.email[0].toUpperCase() %>
                                    </div>
                                    <div>
                                        <h6 class="mb-0 fw-bold">
                                            <%= post.User.Profile ? post.User.Profile.username : post.User.email %>
                                        </h6>
                                        <small class="text-muted" style="font-size: 0.8rem;">
                                            <%= post.formattedDate %>
                                        </small>
                                    </div>
                                </div>

                                <% if (currentUser.id === post.UserId || currentUser.role === 'Admin') { %>
                                <div class="d-flex gap-2">
                                <a href="/pop/<%= post.id %>/edit" class="btn btn-sm text-secondary border-0 p-0" title="Edit">
                                    <i class="bi bi-pencil-square fs-5 hover-warning"></i>
                                </a>

                                <a href="/pop/<%= post.id %>/delete" 
                                class="btn btn-sm text-secondary border-0 p-0" 
                                onclick="return confirm('Hapus post ini?')"
                                title="Hapus">
                                    <i class="bi bi-trash fs-5 hover-danger"></i>
                                </a>
                                </div>
                                <% } %>
                                </div>

                            <p class="card-text fs-5 mb-3" style="white-space: pre-wrap;"><%- formatHashtag(post.content) %></p>

                            <% if(post.picture) { %>
                                <div class="mb-3">
                                    <img src="<%= post.picture %>" class="img-fluid rounded-3 w-100 object-fit-cover shadow-sm" style="max-height: 500px;" alt="Post Image">
                                </div>
                            <% } %>

                            <% if(post.Hashtags && post.Hashtags.length > 0) { %>
                                <div class="hashtag-container mt-3 pt-3 border-top">
                                    <% post.Hashtags.forEach(tag => { %>
                                        <a href="/hashtags/<%= tag.name.replace('#', '') %>" class="btn btn-light btn-sm rounded-pill px-3 me-1 mb-1 border">
                                           <%- tag.name %>
                                        </a>
                                    <% }) %>
                                </div>
                            <% } %>

                        </div>
                    </div>
                <% }) %>

            <% } %>

        </div>
      </div>
    </div>
  </section>

  <footer class="py-4 bg-white border-top text-center mt-auto">
    <div class="container">
      <p class="text-muted mb-0">© <span id="current-year"></span> <strong>Pop. Project</strong>.</p>
    </div>
  </footer>

  <script>
    document.getElementById('current-year').textContent = new Date().getFullYear();
  </script>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
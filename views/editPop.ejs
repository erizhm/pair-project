<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Postingan - Pop</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
  
  <style>
    :root { --pop-color: #f44336; --pop-hover: #d32f2f; }
    body { background-color: #f8f9fa; }
    .btn-pop { background-color: var(--pop-color); border-color: var(--pop-color); color: white; border-radius: 50px; font-weight: 600; }
    .btn-pop:hover { background-color: var(--pop-hover); border-color: var(--pop-hover); color: white; }
    .card-post { border: none; border-radius: 1rem; }
  </style>
</head>
<body>

  <div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-lg-6"> 
            
            <div class="mb-3">
                <a href="/" class="text-decoration-none text-muted small">
                    <i class="bi bi-arrow-left"></i> Batal Edit
                </a>
            </div>

            <div class="card card-post bg-white shadow-sm">
                <div class="card-body p-5">
                    
                    <div class="text-center mb-4">
                        <h3 class="fw-bold">Edit Postingan</h3>
                    </div>

                    <form action="/pop/<%= post.id %>/edit" method="post" enctype="multipart/form-data">
                        
                        <div class="mb-4">
                            <label class="form-label fw-bold text-secondary">Konten</label>
                            <textarea name="content" class="form-control bg-light border-0" rows="6" required><%= post.content %></textarea>
                        </div>
                        
                        <div class="mb-4">
                            <label class="form-label fw-bold text-secondary">URL Gambar</label>
                            <div class="input-group">
                                <span class="input-group-text bg-light border-0"><i class="bi bi-image"></i></span>
                                <input type="file" name="picture" class="form-control bg-light border-0" value="<%= post.picture %>">
                            </div>
                        </div>

                        <div class="d-grid gap-2 mt-5">
                            <button type="submit" class="btn btn-pop btn-lg shadow-sm">
                                Simpan Perubahan
                            </button>
                        </div>

                    </form>

                </div>
            </div>

        </div>
      </div>
    </div>

</body>
</html>